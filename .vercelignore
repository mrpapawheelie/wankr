# Exclude everything except what's needed for frontend build
*
!frontend/
!vercel.json
!package.json
!pnpm-workspace.yaml

# Exclude frontend build artifacts and dependencies
frontend/.next/
frontend/node_modules/
frontend/.env*

# Exclude other unnecessary files
*.log
.DS_Store
